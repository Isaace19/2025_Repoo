/*
Using fscanf, we need to read in a million entries (either through a text file
or keyboard input) So using fscanf and the file open header should be easy.

Attempt: 1
*/

#include <stdio.h>
#include <stdlib.h>